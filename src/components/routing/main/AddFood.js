import { } from "antd";
import React from "react";
import {} from "react-router-dom";

function AddFood({ children }) {
  return (
    <div className="add-food">
        <p>AddFood</p>
      {children}
    </div>
  );
}

export default AddFood;
